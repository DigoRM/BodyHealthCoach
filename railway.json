{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "numReplicas": 1,
      "startCommand": "python manage.py migrate && gunicorn --workers 4 --worker-class gevent --timeout 120 healthbodycoach.wsgi & python manage.py process_tasks",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }